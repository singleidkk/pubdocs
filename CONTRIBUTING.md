# SingleIDドキュメントへのコントリビューション

このリポジトリのドキュメントを追加・更新する場合は、以下のルールに従ってください。用語の表記については、[TERMINOLOGY.md](TERMINOLOGY.md)も参照してください。

## 文書の構成

* 各Markdownファイルには、内容を表すH1見出し（`#`）を1つだけ記載します。
* H1の下は、H2（`##`）、H3（`###`）の順に階層化し、見出しレベルを飛ばさないようにします。
* H1と同じ名称の見出しを直下に重複して記載しません。
* 手順は番号付きリスト、選択肢や確認事項は箇条書きを基本とします。
* 同じ注意事項を複数のセクションで繰り返さず、共通する位置へ集約します。

## 文書更新日

* 文書更新日を記載する場合は、H1の直下に`文書更新日:YYYY-MM-DD`の形式で記載します。
* 本文の内容を確認または更新した日を記載します。
* タイトル追加や機械的な書式変更だけを行った場合は、文書更新日を変更しません。
* 過去の更新日が確認できない場合は、推測した日付を追加しません。

## 画面と操作項目の表記

* 画面遷移は太字で記載します。

  ```md
  **SingleID 管理者ポータル＞認証＞RADIUS＞簡易設定**画面
  ```

* 画面名、タブ名、ボタン名、入力項目名など、UIに表示される名称は太字で記載します。

  ```md
  **基本情報**タブで**登録**ボタンをクリックします。
  ```

* メニューの区切りには`＞`を使用します。
* UIの名称は、実際の画面表示に合わせます。
* 外部サービスの画面名や操作項目は、そのサービスの公式表記を優先します。

## 技術用語と設定値

* RFCや製品仕様で定義された正式名称を使用します。例えば、RADIUS属性は`NAS-IP-Address`、`NAS-Identifier`と記載します。
* コマンド、設定値、属性値、エラーメッセージ、ファイル名はインラインコードで記載します。

  ```md
  属性値に `0.0.0.0` を設定します。
  ```

* 製品固有のUI表記と正式な技術用語が異なる場合は、文脈に応じて使い分けます。

## info、warning、danger

* 補足情報には`info`、注意が必要な操作や制約には`warning`、復元できない操作や重大な影響には`danger`を使用します。
* 同じ場所に複数のadmonitionを連続して置かず、関連する内容は1つにまとめます。
* 長い補足は本文のセクションとして記載し、admonitionへ情報を詰め込みすぎないようにします。

## リンクとナビゲーション

* リポジトリ内の文書には相対リンクを使用します。
* 見出しへのリンクを追加した場合は、MkDocsで生成されるアンカーを確認します。
* 見出し名を変更または削除する場合は、旧アンカーを参照している文書がないか確認します。
* `mkdocs.yml`の`nav`には、サイトのナビゲーションへ常時表示するページだけを追加します。
* 他のページから参照される設定ガイドなどは、必ずしも`nav`へ追加する必要はありません。

## リリースノート

* 製品版と評価版で同じ変更を提供する場合は、両方のリリースノートへ同じ表記で記載します。
* 画面遷移、画面名、タブ名、操作名は、通常のドキュメントと同じ表記ルールを使用します。
* 新機能または改善と、不具合修正を分けて記載します。
* 内部実装の変更だけで利用者の操作や仕様が変わらない場合は、管理者ガイドへ追記する必要はありません。
* 公開済みのリリース内容を変更する場合は、変更の意味を変えずに表記のみを修正します。

## ローカルでの確認

Python環境へ、GitHub Actionsで使用しているパッケージをインストールします。

```shell
python -m pip install mkdocs mkdocs-material fontawesome_markdown
```

文書をビルドします。

```shell
mkdocs build
```

ブラウザで確認する場合は、開発サーバを起動します。

```shell
mkdocs serve
```

変更をコミットする前に、少なくとも以下を確認してください。

```shell
git diff --check
mkdocs build
```

ビルド時に`nav`へ含まれていないページが表示されることがあります。意図的に他のページから参照している文書であれば、警告だけを理由に`nav`へ追加する必要はありません。
